1. Ejecutar el archivo .sql SCRIPTBD para la creacion de bd y tablas
2. Levantar docker-compose up -d en el proyecto events.backend
3. Para pruebas locales se dejo habilitado swagger para pruebas de los endpoints
4. Una vez levantado docker-compose up -d en el proyecto events.backend puedes hacer pruebas locales levantando el frontend (react + nextjs) junto a los dos microservicios
5. Para iniciar sesion en el frontend userName = admin y password = admin para fines de pruebas locales
